import { PenerimaTable } from "@/components/penerima/PenerimaTable";

export const metadata = {
  title: "Dashboard · Kupon Kurban",
};

export default function DashboardPage() {
  return <PenerimaTable />;
}
